{
  "$schema": "https://docs.anthropic.com/claude-code/hooks-schema.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "test -z \"${ENGRAM_DISABLE_LOG:-}\" || exit 0; case \"${TOOL_INPUT_COMMAND:-}\" in *engram*) :;; *) exit 0;; esac; exit 0"
          }
        ]
      }
    ]
  }
}
